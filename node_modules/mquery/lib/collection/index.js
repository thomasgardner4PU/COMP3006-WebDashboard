'use strict';

const env = require('mquery/lib/env');

if ('unknown' == env.type) {
  throw new Error('Unknown environment');
}

module.exports =
  env.isNode ? require('mquery/lib/collection/node') :
    env.isMongo ? require('mquery/lib/collection/collection') :
      require('mquery/lib/collection/collection');

